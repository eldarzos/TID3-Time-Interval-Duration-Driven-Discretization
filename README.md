# TID3-Time-Interval-Duration-Driven-Discretization
ID3 (Time Interval Duration Driven Discretization): a supervised discretization method that selects cutoffs by maximizing the cross-population divergence of Symbolic Time Interval (STI) duration distributions. Includes a UEA loader and an end-to-end pipeline emitting discretization states and the STI series for TIRP mining.
